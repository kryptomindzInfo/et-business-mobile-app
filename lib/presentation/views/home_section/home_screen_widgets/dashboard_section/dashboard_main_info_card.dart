import 'package:etbank_business_app/presentation/views/common_widgets/info_card_common_widget.dart';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

class DashboardInfoCardWidget extends ConsumerWidget {
  const DashboardInfoCardWidget({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return const InfoCardCommonWidget(
        child: Column(
      children: [
        // 20.spaceY,
        
        // 20.spaceY,
      ],
    ));
  }
}
